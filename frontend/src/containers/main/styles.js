import styled from "styled-components";

export const Container = styled.div`
  padding: 2rem;
  max-width: 1200px;
  margin: 0 auto;
`;

export const SearchWrapper = styled.div`
  width: 100%;
  max-width: 902px; // 902px is coming from the figma design, however its fully responsive if the screen size gets smaller it will adjust
  margin: 0 auto 40px;
`;
